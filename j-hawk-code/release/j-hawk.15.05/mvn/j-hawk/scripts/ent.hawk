#include xml-struct LoginInfo "/home/manoranjan/jax/login1.xml"

function main()
{
    for(var i=1; i <50; i = i+1)
    {
        LoginInfo loginInfo
        exec login(loginInfo)
    }
}

function login(LoginInfo loginInfo)
{

    exec TellurideHttpModule->Execute(loginInfo)
}